# readme-rag-eval

내 프로젝트 문서 12건을 검색하고 SQLite에 질의해 답하는, 도구 연동형 LLM 시스템.
**질문 20개에 정답을 라벨링해 검색·도구 선택·생성 정확도를 따로 측정했다.**

구현만으로는 "LLM을 붙여 봤다"에서 끝난다. 이 저장소의 내용은 무엇이 얼마나
맞았고, 틀린 것이 **어디서** 틀렸는지를 나눈 표다.

## 결과

측정 대상은 **Qwen2.5-7B-Instruct**를 사내 GPU 서버(L40S 1장)에 vLLM으로 올린 것이다.
질문 20개, 같은 설정으로 4회 반복했을 때 **문항 단위까지 동일**했다(아래 "재현성").

### 도구 구성 ablation

| 조건 | 도구 구성 | Recall@3 | 정답률 | 도구 선택 정확도 | 평균 호출 | 도구 에러율 | 평균 지연 |
|---|---|---|---|---|---|---|---|
| **C** | 둘 다 | 0.467 | **0.450** | 0.800 | 1.1 | 0.000 | 5.7s |
| A | 검색만 | 0.667 | 0.400 | 0.700 | 0.8 | 0.000 | 5.9s |
| B | SQL만 | -- | 0.300 | (1.000) | 0.9 | 0.111 | 4.7s |

B의 도구 선택 정확도 1.000은 고를 도구가 하나뿐이라 자동으로 1이다. 지표로 읽으면 안 된다.

**가설은 절반만 맞았다.** "도구를 둘로 나누는 편이 낫다"는 B(SQL만) 대비로는
분명하다 — 20문항 중 3문항 차이다. 반면 A(검색만) 대비 C의 우위는 **1문항**이고,
이는 아래 재현성 절에서 잡은 측정 해상도(±1~2문항) 안에 있다. **이 표본에서
"C가 A보다 낫다"고는 말할 수 없다.**

이유는 자료 쪽에 있다. 이 README들은 수치를 **본문 표**로 싣고 있어서, 검색만으로도
숫자 질문이 꽤 답해진다. 문서에 표가 없고 DB에만 수치가 있는 코퍼스였다면 A는 훨씬
낮게 나왔을 것이다. 즉 이 결과는 도구 분리가 쓸모없다는 뜻이 아니라, **분리의 이득은
문서와 DB가 얼마나 겹치느냐에 달려 있다**는 뜻이다.

### 조건별 실패 분포

| 조건 | 검색 실패 | 도구 구성 한계 | 도구 미사용 | 도구 선택 실패 | 생성 실패 |
|---|---|---|---|---|---|
| C | 0 | 0 | 2 | 2 | 7 |
| A | 1 | 5 | 4 | 0 | 2 |
| B | 0 | 3 | 6 | 0 | 5 |

A의 "도구 구성 한계" 5건은 검색만으로는 닿지 않는 집계 질문이고, B의 6건은
문서를 봐야 하는 서술형 질문이다. 도구를 하나로 줄이면 반대쪽 질문이 통째로
무너진다는 것 — 가설 중 이 부분은 표에 그대로 나타났다.

### 실패 분석 — 유형별 원인과 조치

조건 C(둘 다) 기준, 틀린 11문항의 분류다. 유형마다 고칠 곳이 다르다.

| 유형 | 건수 | 원인 | 고친 것 |
|---|---|---|---|
| 검색 실패 | 0 | 정답 문단이 상위 k에 없음 | 한→영 질의 확장으로 Recall@3 0.667 → 1.000 (아래 "고친 것 2") |
| 도구 미사용 | 2 | 도구를 한 번도 부르지 않고 답함 | 시스템 프롬프트 교체로 8건 → 2건 (아래 "고친 것 1") |
| 도구 선택 실패 | 2 | 서술형 질문에 `query_db`를 씀 | 도구 description에 "수치 비교·집계는 query_db" 명시. 남은 2건은 미해결 |
| 생성 실패 | 7 | 근거를 받고도 답이 틀림 | 미해결. 내역은 아래 "남은 실패에서 본 것" |

**고칠 곳이 BM25가 아니라는 게 이 표의 요지다.** 검색 실패가 0건이므로 검색기를
더 손봐도 정답률은 오르지 않는다. 실제로 효과가 난 조치도 둘 다 검색기 바깥이었다.

### 고친 것 1 — 프롬프트 한 문장이 도구 호출을 통째로 없앴다

처음 쓴 시스템 프롬프트에는 `"도구로 확인되지 않으면 모른다고 답하라"`가 있었다.
의도는 환각 방지였는데, 7B 모델은 이것을 **도구를 부르기 전에** 모른다고 답해도
된다는 허가로 읽었다. 문장을 하나씩 붙여 가며 도구 호출 여부를 셌다(각 5문항):

| 시스템 프롬프트 | 도구 호출 |
|---|---|
| 역할만 | 5/5 |
| + 근거 인용 지시 | 5/5 |
| + **"확인되지 않으면 모른다고 답하라"** | 1/5 |
| + 수치 보존 지시 (= v0 전체) | 0/5 |
| 역할 + 근거 + 수치 (문제 문장만 제거) | 5/5 |
| **v2: "도구를 한 번도 부르지 않은 채로 모른다고 답해서는 안 된다"** | **5/5** |

같은 20문항을 v0과 v2로 돌린 결과:

| 프롬프트 | 도구 미사용 | Recall@3 | 도구 선택 정확도 | 정답률 |
|---|---|---|---|---|
| v0 | 8건 | 0.133 | 0.500 | 0.350 |
| **v2** | **2건** | **0.467** | **0.800** | **0.450** |

환각 방지 의도를 버리지 않고 문장만 바꿔서 얻은 차이다.
**v0으로 그냥 돌렸다면 "7B 모델은 도구를 잘 못 쓴다"는 잘못된 결론을 냈을 것이다.**

### 고친 것 2 — 한국어 질문이 영어 문서를 못 찾았다

문서 12건 중 9건이 영어 README라 "초해상도"로는 "super-resolution"이 걸리지 않는다.
손으로 쓴 별칭표로 질의를 확장했다(검색기 단독, 라벨 15문항):

| 질의 확장 | Recall@1 | Recall@3 | Recall@5 |
|---|---|---|---|
| off | 0.333 | 0.667 | 0.800 |
| **on** | **0.667** | **1.000** | **1.000** |

### 남은 실패에서 본 것

생성 실패 7건을 하나씩 읽었다. 라벨이 엄격해서 틀린 것은 1건뿐이고 나머지는 진짜 오답이다.

- **수치 환각** — DB에 기준값 28.15가 있는데 "31.55"를 지어내고 차이를 0.02라고 답했다.
  도구가 값을 돌려줬는데도 생성 단계에서 바뀌었다.
- **스키마가 부른 오답** — `started`/`ended`를 TEXT로 뒀더니 모델이 `ended - started`로
  정렬했다. SQLite는 에러 없이 문자열을 숫자로 강제변환해 **조용히 틀린 답**을 준다.
  `julianday()`를 쓰게 하려면 도구 description이나 스키마를 바꿔야 한다.
- **있는 데이터를 없다고 함** — 2건. SQL이 한 번 빗나가면 재시도 없이 포기했다.
- **표 오독** — 임펄스 잡음 최적 필터를 median(43.92 dB) 대신 Bilateral C=75라고 답했다.
- **라벨이 엄격했던 1건** — 조건 이름은 정확히 맞혔는데 값(43.92)을 안 적어 오답 처리됐다.
  `must_include`를 문자열 일치로 잡은 대가이고, 결과를 보고 라벨을 고치면 측정이 아니게
  되므로 그대로 뒀다.

### 재현성

같은 설정(v2, 온도 0)으로 4회 반복했다. **20문항의 정답/오답 판정, 첫 호출 도구,
Recall@3이 전부 동일**했다(편차 0.000). 커밋된 `results/agent_*_run1.csv`와
`*_run2.csv`를 비교하면 확인된다.

완전히 같지는 않았다. 이미 오답이던 한 문항(B 조건 d1)에서 **재시도 횟수만 4회와
5회로 갈렸다.** 모델이 SQL을 고쳐 가며 여러 번 시도하다 실패하는 문항이라, 지표에는
영향이 없지만 결정적이지는 않다는 증거다.

그리고 이 네 번보다 이른 실행 한 번은 C 조건에서 2문항이 달랐다(0.350 대 0.450).
그래서 완전한 재현성을 주장하지 않고 **이 측정의 해상도를 ±1~2문항(±0.05~0.10)으로
본다.** 위에서 C와 A의 차이를 결론으로 쓰지 않은 근거가 이것이다.

### 비용·지연 (로컬 GPU)

| | Qwen2.5-7B (L40S 1장, vLLM) |
|---|---|
| 질문당 지연 | 5.7s (C 조건 평균) |
| 20문항 토큰 | 입력 47,509 / 출력 5,553 |
| 금전 비용 | 없음 (사내 GPU) |

Anthropic API 행은 **비우지 않고 비워 둔 이유를 적는다** — 이 PC에 API 키가 없고
API는 유료라, 키 없이 돌릴 수 있는 로컬 트랙만 측정했다. `agent.py --backend anthropic`
경로는 구현돼 있고 `backends.py`가 두 형식의 차이를 흡수한다.

## 무엇을 만들었나

```
질문
 +-> [LLM 루프] --- tool_use ---> search_docs(query, k) --> docs/*.md 청크 BM25 상위 k개
       ^                     +--> query_db(sql)          --> SQLite (repos, metrics)
       |                                                        |
       +----------------- tool_result <-------------------------+

 최종 답변 = 텍스트 + 인용한 문단/행
```

| 파일 | 역할 |
|---|---|
| [`retriever.py`](retriever.py) | 마크다운 헤딩 단위 청킹 + BM25. 한글 2-gram 토큰화 |
| [`aliases.py`](aliases.py) | 한국어 질문 → 영어 문서를 잇는 질의 확장표 |
| [`build_db.py`](build_db.py) | `repos` / `metrics` 두 테이블 적재 (수치는 손으로 옮김) |
| [`tools.py`](tools.py) | `search_docs` / `query_db` 구현과 도구 스키마 |
| [`backends.py`](backends.py) | Anthropic API와 OpenAI 호환(vLLM)을 한 인터페이스 뒤에 |
| [`agent.py`](agent.py) | `stop_reason`을 보며 직접 도는 도구 호출 루프 |
| [`eval/`](eval/) | 라벨 20문항 + 측정 스크립트 |
| [`scripts/serve_vllm.py`](scripts/serve_vllm.py) | JupyterHub 커널로 온프레미스 GPU에 vLLM 기동 |

## 설계 판단

### 1. 도구를 문서 검색과 SQL로 나눈 이유

서술형 질문("왜 이 필터를 골랐나")과 집계 질문("PSNR이 30dB를 넘는 프로젝트를
전부")은 필요한 자료구조가 다르다. 전자는 문단이, 후자는 정렬 가능한 행이 있어야
한다. 한쪽으로만 만들면 반대쪽 질문에서 무너진다 — 라는 것이 **가설**이었고,
도구 구성 A(검색만) / B(SQL만) / C(둘 다)를 같은 20문항에 돌려 확인했다.

### 2. 한글 토큰화를 2-gram으로 한 이유

형태소 분석기는 외부 의존성과 설치 실패 지점을 늘린다. 2-gram은 `pip install`
없이 재현되고, `PSNR`·`Q1.15` 같은 짧은 기술 용어에서 오히려 안전하다.

### 3. 한→영 질의 확장을 붙인 이유 (계획에 없던 것)

문서 12건 중 **9건이 영어 README**다. BM25는 어휘 일치만 보므로 "초해상도"로는
"super-resolution"이 한 건도 걸리지 않는다. 계획 단계에서는 몰랐고, 검색기를
단독으로 측정하자마자 드러났다.

| 질의 확장 | Recall@1 | Recall@3 | Recall@5 |
|---|---|---|---|
| off | 0.333 | 0.667 | 0.800 |
| **on** | **0.667** | **1.000** | **1.000** |

라벨이 붙은 15문항 기준 (`python eval/run_eval.py --retrieval-only`).
임베딩 대신 손으로 쓴 58항목 표를 고른 이유는 항목 수가 적고, 무엇이 왜 걸렸는지
한 줄로 설명되기 때문이다.

### 4. LLM이 생성한 SQL을 어떻게 막았나

두 겹이다.

1. `SELECT`로 시작하지 않거나 쓰기 키워드(`insert|update|delete|drop|...`)가
   보이면 거절하고, 세미콜론으로 문을 이어 붙이는 것도 막는다.
2. 연결 자체를 읽기 전용으로 연다 — `sqlite3.connect("file:meta.db?mode=ro", uri=True)`.

**실제 방어선은 2번이다.** 1번은 통과하지만 의도와 다른 SELECT(예: `UNION`으로
상수 행 붙이기)는 여전히 가능하고, 실제로 통과하는 것을 확인했다. 문자열 검사만으로
막았다고 말할 수 없어서 연결 수준에서 한 겹 더 둔 것이다.

### 5. 루프를 직접 쓴 이유

SDK의 `tool_runner`가 대신 돌려주지만, 이 프로젝트에서 측정하려는 대상이
루프 안에서 오가는 것(어떤 도구를 먼저 불렀나, 몇 번 불렀나, 도구가 몇 번
실패했나)이다. 직접 들고 있어야 `Trace`로 뽑을 수 있다.

루프에서 실제로 틀리기 쉬운 곳 세 가지:

1. `tool_result`의 `tool_use_id`는 해당 `tool_use`의 `id`와 정확히 같아야 한다.
2. 한 응답에 `tool_use`가 여러 개 오면 **모든** `tool_result`를 **하나의** 메시지에
   담는다. 쪼개 보내면 모델이 병렬 호출을 그만둔다. (OpenAI 호환 쪽은 반대로
   도구마다 `role: "tool"` 메시지를 따로 보낸다 — `backends.py`가 이 차이를 흡수한다.)
3. 실패한 도구도 결과를 빼먹지 않고 `is_error`로 돌려준다.

## 데이터

`donghyeoni` 계정의 프로젝트 레포 12개 README(`docs/*.md`)와, 거기서 손으로 옮긴
메타DB. README 파싱을 자동화하지 않은 이유는 12개뿐이고 표 형식이 제각각이라
파서가 조용히 틀리기 때문이다.

**`docs/*.md`는 이 저장소에 넣지 않는다.** 원본이 각 레포에 이미 공개돼 있는 사본이라
두 곳에서 갈라지기 때문이다. 대신 `python scripts/fetch_docs.py`(내부적으로
`gh api repos/<user>/<repo>/readme`)로 언제든 같은 상태를 만든다 — 측정에 쓴 12건과
바이트 단위로 같은 파일을 받는 것을 확인했다.

- 청크 110개 / 11개 레포 (`Algorithm-training`은 README가 한 줄이라 청크가 없다 —
  DB에는 있고 문서 검색에는 없는 레포가 하나 생긴다)
- `repos` 12행, `metrics` 63행

```sql
repos(name, language, domain, started, ended, summary)
metrics(repo, name, condition, baseline, value, unit)
```

## 사용법

```bash
pip install -r requirements.txt
python scripts/fetch_docs.py   # 검색 대상 README 12건 수집 (gh CLI 필요)
python build_db.py
```

검색기만 확인 (LLM 불필요):

```bash
python retriever.py "초해상도 중요도 맵"
python eval/run_eval.py --retrieval-only
```

질문 하나 던지기:

```bash
python agent.py "PSNR이 30dB를 넘은 프로젝트 알려줘" --backend vllm:http://localhost:8000 --trace
```

전체 평가 (도구 구성 A/B/C):

```bash
python eval/run_eval.py --backend vllm:http://localhost:8000 --toolsets A,B,C
```

## 온프레미스 GPU 트랙

사내 GPU 서버(L40S ×4, JupyterHub만 열려 있고 SSH는 없음)에 vLLM을 올려 같은
평가를 돌렸다. `scripts/serve_vllm.py`가 JupyterHub 커널 WebSocket으로 원격
명령을 보낸다.

```bash
export JUP_HOST=... JUP_USER=... JUP_TOKEN=...
python scripts/serve_vllm.py gpus      # 빈 GPU 확인
python scripts/serve_vllm.py setup     # 홈에 전용 venv 만들고 vLLM 설치
python scripts/serve_vllm.py up --gpu 0
python scripts/serve_vllm.py status
```

공용 파이썬(`/opt/tljh/user`)에 설치하지 않고 홈에 venv를 따로 판 이유는 그
인터프리터를 다른 사용자가 함께 쓰기 때문이다. GPU도 비어 있는 장치(0번)만 골라
썼다 — 1번은 다른 작업이 41GB를 쓰고 있었다.

### 버전 고르기 — 드라이버가 상한을 정한다

`pip install vllm`으로 최신판(0.29.0)을 깔면 `torch 2.13.0+cu130`이 따라오는데,
이 서버 드라이버는 `570.211.01`(CUDA 12.8)이라 엔진이 뜨지 않는다:

```
RuntimeError: The NVIDIA driver on your system is too old (found version 12080)
```

vLLM 휠은 파일명에 CUDA 버전이 없어서, **각 vLLM이 고정한 torch의 PyPI 기본 CUDA**로
거꾸로 판별했다.

| vLLM | 고정 torch | torch의 PyPI 기본 CUDA | 이 드라이버에서 |
|---|---|---|---|
| 0.27 ~ 0.29 | 2.13.0 | cu130 | X |
| 0.20 ~ 0.26 | 2.11.0 | cu13x | X |
| **0.17 ~ 0.19** | **2.10.0** | **`nvidia-cuda-runtime-cu12==12.8.90`** | **O** |

그래서 `vllm==0.19.1`로 고정했다. 중간에 `torch`만 cu128로 내려도 안 되는데,
**vLLM의 컴파일된 확장 자체가 CUDA 13으로 빌드**돼 있어 `libcudart.so.13`을 찾기
때문이다. 또 제자리 다운그레이드는 이전 설치의 `nvidia-*-cu13` 패키지가 남아
섞이므로, venv를 지우고 새로 만드는 편이 빠르다.

드라이버를 올리는 선택지는 공용 서버라 건드리지 않았다.

## 한계

- 문서 12건, 질문 20개다. 이 크기에서 나온 수치는 방법이 통한다는 증거이지
  일반적인 성능 수치가 아니다.
- `must_include` 문자열 일치로 정답을 판정한다. 실제로 20문항 중 1건이 이 때문에
  오답으로 잡혔다(조건 이름은 맞고 값을 안 적음). 결과를 보고 라벨을 고치지 않았다.
- 조건 C와 A의 차이는 1문항이라 측정 해상도 안에 있다. 이 저장소는 "도구를 나누면
  낫다"를 증명하지 못했고, 못 했다는 것까지가 결과다.
- 모델은 Qwen2.5-7B 하나만 측정했다. 더 큰 모델에서 도구 선택 실패가 어떻게 변하는지는
  다루지 않았다.
- 질의 확장표는 이 12개 문서를 보고 손으로 썼다. 문서가 바뀌면 표도 손봐야 한다.
